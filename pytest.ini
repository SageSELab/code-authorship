[pytest]
testpaths = adversarial-rule-unit-tests
python_files = test_*.py
addopts = -ra

# Keep the cache out of the repository. In the Docker images the working tree
# is a bind mount owned by the host user, which the container user cannot
# necessarily write to; pytest would otherwise warn on every run.
cache_dir = /tmp/.pytest_cache
