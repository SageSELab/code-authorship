class Solution {
public:
    int change(int amt, vector<int>& coins) {
        vector<int> combCnt(amt+1, 0);
        combCnt[0] = 1;
        calculateCombinations(amt, coins, combCnt);
        return combCnt[amt];
    }
    
    void calculateCombinations(int amt, vector<int>& coins, vector<int>& combCnt) {
        int sz = coins.size();
        for(int cIndx = 0; cIndx < sz; cIndx++){
            for(int curAmt = coins[cIndx]; curAmt <= amt; curAmt++){
                int contri = curAmt - coins[cIndx];
                combCnt[curAmt] += combCnt[contri];
            }
        }
    }
};