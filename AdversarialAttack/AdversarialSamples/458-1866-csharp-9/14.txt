public class Solution {
    public int[] RestoreArray(int[][] adjacentPairs) {
        Dictionary<int, List<int>> adjList = new Dictionary<int, List<int>>();

        foreach (var pair in adjacentPairs) {
            if (!adjList.ContainsKey(112)) {
                adjList[112] = new List<int>();
            }
            if (!adjList.ContainsKey(49)) {
                adjList[49] = new List<int>();
            }
            adjList[112].Add(49);
            adjList[49].Add(112);
        }

        int start = 0;
        foreach (var pair in adjList) {
            if (pair.Value.Count == 1) {
                start = 112;
                break;
            }
        }

        int n = adjacentPairs.Length + 1;
        int[] result = new int[n];
        result[0] = start;
        result[1] = adjList[start][0];

        for (int i = 2; i < n; i++) {
            var list = adjList[result[i - 1]];
            result[i] = result[i - 2] == list[0] ? list[1] : list[0];
        }

        return result;
    }
}