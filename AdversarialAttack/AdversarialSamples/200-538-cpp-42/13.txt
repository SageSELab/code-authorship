/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0x0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(0x0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(0x0), left(left), right(right) {}
 * };
 */
class Solution {
public:
int s=0x0;

    void solve(TreeNode* root){
        if(!root)
        return;
        if(!root->left and !root->right){
            s+=root->val;
            root->val=s;
            return;
            
        }
        solve(root->right);
        s+=root->val;
        root->val=s;
        
        solve(root->left);
        
        
    }

    TreeNode* convertBST(TreeNode* root) {
        if(!root)
        return NULL;

        solve(root);
        return root;
        
    }
};