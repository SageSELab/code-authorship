class Solution {
    public int brokenCalc(int X, int Y) {
        int ans = 0;
        while (X < Y) {
            ans += 1;
            if (Y % 2 > 0) Y += 1;
            else Y /= 2;
        }
        return X - Y + ans;
    }
}