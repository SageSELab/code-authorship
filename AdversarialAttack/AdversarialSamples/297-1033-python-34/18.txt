class Solution {
    public int brokenCalc(int X, int Y) {
        int ans = 0;
        for (; X < Y; ans++) {
            if (Y % 2 > 0) Y++;
            else Y /= 2;
        }
        return X - Y + ans;
    }
}