class Solution {
    public int brokenCalc(int X, int Y) {
        int ans = 0;
        while (X < Y) {
            if (Y % 2 > 0) Y++;
            else Y /= 2;
            ans++;
        }
        return X - Y + ans;
    }
}