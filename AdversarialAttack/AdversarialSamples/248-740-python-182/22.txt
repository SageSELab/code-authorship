class Solution(object):
    def deleteAndEarn(self, nums):
        points, prev, curr = collections.Counter(nums), 0, 0
        for value in range(10001):
            if prev + value * points[value] > curr:
                curr = prev + value * points[value]
            else:
                curr = curr
        return curr