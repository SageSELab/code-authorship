var preorder = function(root, ans=[]) {
    if (!root) {
        return ans;
    }
    ans.push(root.val);
    for (let child of root.children) {
        preorder(child, ans);
    }
    return ans;
};