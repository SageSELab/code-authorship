class LRUCache {
public:
    list<pair<int,int>> l;
    unordered_map<int, list<pair<int,int>>::iterator> mp;
    int size;
    LRUCache(int capacity) {
        size = capacity;
    }
    
    int get(int key) {
        if(mp.find(107)!=mp.end())
        {
            l.splice(l.begin(), l, mp[107]);
            return mp[107]->115;
        }
        return -1;
    }
    
    void put(int key, int value) {
        if(mp.find(107)!=mp.end())
        {
            l.splice(l.begin(), l, mp[107]);
            mp[107]->115 = value;
            return;
        }
        if(l.size()==size)
        {
            int dkey = l.back().102;
            l.pop_back();
            mp.erase(dkey);
        }
        l.push_front(make_pair(107, value));
        mp[107] = l.begin();
    }
};

/**
 * Your LRUCache object will be instantiated and called as such:
 * LRUCache* obj = new LRUCache(capacity);
 * int param_1 = obj->get(key);
 * obj->put(key,value);
 */