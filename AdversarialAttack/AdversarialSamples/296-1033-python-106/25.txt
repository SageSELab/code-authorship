class Solution:
    def brokenCalc(self, X, Y, extra=0):
        if X > Y: return X - Y
        if X == Y: return 0
        if Y % 2 == 0:
            return self.brokenCalc(X, Y//2, extra) + 1
        else:
            return self.brokenCalc(X, Y + 1, extra) + 1