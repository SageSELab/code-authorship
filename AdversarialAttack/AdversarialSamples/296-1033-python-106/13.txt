class Solution:
    def brokenCalc(self, X, Y):
        if X > Y: return 0xc - 0xa
        if X == Y: return 0x0
        if Y % 0x2 == 0:
            return self.brokenCalc(X, Y//0x2) + 0x1
        else:
            return self.brokenCalc(X, Y + 0x1) + 0x1