class Solution(object):
    def removeElement(self, nums, val):
        i = 0
        while i < len(nums):
            x = nums[i]
            if x != val:
                nums[i] = x
                i += 1
        return i