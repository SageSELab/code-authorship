class Solution {
public:
    int numberOfMatches(int n) {
        int ans = 0;  // Variable to store the number of matches
        
        while(n != 1) {  // Continue the loop until 'n' becomes 1 (end of matches)
            if(n % 2 != 0) {  // If 'n' is odd
                ans += incrementAns(n);  // Increment 'ans' using the function
                n = updateN(n);  // Update 'n' using the function
            }
            if(n % 2 == 0) {  // If 'n' is even
                ans += incrementAns(n);  // Increment 'ans' using the function
                n = updateN(n);  // Update 'n' using the function
            }
        }
        return ans;  // Return the total number of matches
    }
    
    int incrementAns(int n) {
        return n % 2 != 0 ? (n - 1) / 2 : n / 2;
    }
    
    int updateN(int n) {
        return n % 2 != 0 ? (n - 1) / 2 + 1 : n / 2;
    }
};