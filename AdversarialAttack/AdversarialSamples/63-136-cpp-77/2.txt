class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int res=0;
        cout << "res initialized: " << res << endl;
        for(int i=0;i<nums.size();i++){
            res^=nums[i];
            cout << "res updated: " << res << endl;
        }
        return res;
    }
};