class Solution {
public:
    vector<int> singleNumber(vector<int>& nums) {
        map<int, int>m;
        for(auto it: nums){m[it]++;}
        vector<int>ans;
        for(int i=0; i<nums.size(); i++){if(m[nums[i]]!=2){ans.push_back(nums[i]);}}
        return ans;
    }
};