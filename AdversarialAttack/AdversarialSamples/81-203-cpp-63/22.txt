class Solution {
public:
    ListNode* removeElements(ListNode* head, int T) {
        if(!head) {
            return nullptr;
        }
        head -> next = removeElements(head -> next, T);
        if(head -> val == T) {
            return head -> next;
        } else {
            return head;
        }
    }
};