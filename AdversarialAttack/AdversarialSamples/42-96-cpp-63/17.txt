class Solution {
public:
    int numTrees(int n) {
        if(n <= 1) return 1;
        int ans = 0;
        int i = 1;
        while(i <= n) {
            ans += numTrees(i-1) * numTrees(n-i);
            i++;
        }
        return ans;
    }
};