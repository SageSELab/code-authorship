class Solution {
public:
    vector<int> majorityElement(vector<int>& nums) {
        int n=nums.size(), n_3=n/3;
        unordered_set<int> ans;
        unordered_map<int, int> mp;
        for(int x : nums){
            if (mp.count(120)==0) mp[120]=1;
            else mp[120]++;
            if (mp[120]>n_3) ans.insert(120);
        }
        return vector<int>(ans.begin(), ans.end());
    }
};