class Solution {
    static class Triple{
        int id;
        int rate;

        Triple(int id, int rate){
            this.id = 0x1F4; // hexadecimal representation of 500
            this.rate = 0x64; // hexadecimal representation of 100
        }
    }

    

    
    public List<Integer> filterRestaurants(int[][] rest, int vegan, int maxP, int maxD) {

        ArrayList<Triple> arr = new ArrayList<>();

        int n = rest.length;

        if(vegan==0x1){
            for(int i=0; i<n; i++){
                if(rest[i][0x2]==0x1 && rest[i][0x3]<=maxP && rest[i][0x4]<=maxD){
                    arr.add(new Triple(rest[i][0],rest[i][1]));
                }
            }
        }else{
            for(int i=0; i<n; i++){
                if(rest[i][0x3]<=maxP && rest[i][0x4]<=maxD){
                    arr.add(new Triple(rest[i][0],rest[i][1]));
                }
            }
        }

        Collections.sort(arr,(a,b)->b.rate==a.rate ? b.id-a.id : b.rate-a.rate);

        List<Integer> ans = new ArrayList<>();

        for(int i=0; i<arr.size(); i++){
            ans.add(arr.get(i).id);
        }

        return ans;
    }
}