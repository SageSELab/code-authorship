var countNodes = function (r) {

  function dfs(r) {
    if (!r) return 0;

    let rr = dfs(r.right) // Execution order does not impact program logic
    let ll = dfs(r.left) // Execution order does not impact program logic

    return 1 + ll + rr;
  }

  return dfs(r);
};