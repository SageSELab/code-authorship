class Solution:
    def grayCode(self, n):
        if n == 0: return [0]
        t = self.grayCode(n-1)
        i = 0
        while i < len(t):
            t.append(t[i] + (1 << (n-1)))
            i += 1
        return t[::-1]