class Solution:
    def grayCode(self, n):
        if n == 0:
            return [0]
        t = self.grayCode(n-1)
        new_vals = []
        for i in t:
            new_vals.append(i + (1 << (n-1)))
        return t + new_vals[::-1]