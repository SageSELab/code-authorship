class Solution:
    def grayCode(self, n):
        if n == 0: 
            print(f"n: {n}")
            return [0]
        t = self.grayCode(n-1)
        print(f"t: {t}")
        result = t + [i+(1<<(n-1)) for i in t][::-1]
        print(f"result: {result}")
        return result