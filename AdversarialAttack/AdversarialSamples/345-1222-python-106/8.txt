class Solution:
    def removeCoveredIntervals(self, intervals):
        intervals.sort(key = lambda x: (x[1], -x[0]))
        left, rem, n = -1, 0, len(intervals)
        for start, _ in intervals:
            if start >= left:
                rem += 1
            else:
                left = start
        return n - rem