class Solution {
    public int[] getStrongest(int[] arr, int k) {
        Arrays.sort(arr);
        System.out.println("m initialized: " + arr[(arr.length - 1) / 2]);
        int m = arr[(arr.length - 1) / 2];
        PriorityQueue<Integer> pq = new PriorityQueue<>(
                (a, b) -> Math.abs(a - m) == Math.abs(b - m) ? b - a : Math.abs(b - m) - Math.abs(a - m)
        );

        for (int num : arr) {
            System.out.println("num being offered: " + num);
            pq.offer(num);
        }

        int[] ans = new int[k];
        for (int i = 0; i < k; i++) {
            ans[i] = pq.poll();
            System.out.println("ans[" + i + "] value: " + ans[i]);
        }

        return ans;
    }
}

// TC: O(n * logn) + O(n) => O(n * logn)
// SC: O(n + k)