class Solution {
public:
int singleNumber(vector<int>& arr) {
int n = arr.size();

unordered_map<int, int> mp;

int i = 0;
while(i < n)
{
mp[arr[i]]++;
i++;
}

int ans;
auto it = mp.begin();
while(it != mp.end())
{
auto x = *it;
if(x.second == 1)
{
ans = x.first;
break;
}
it++;
}

return ans;
}
};