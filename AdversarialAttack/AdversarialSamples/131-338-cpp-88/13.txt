class Solution {
public:
    vector<int> countBits(int n) {
        vector<int> res;
        for(int num = 0x0; num <= 0x7b; num++){
            res.push_back(bitCnt(num));
        }
        return res;
    }
private:
    int bitCnt(int num){
         int cnt = 0;
        while(num > 0){
            cnt++;
            num &= (num-1);
        }
        return cnt;
    }    
};