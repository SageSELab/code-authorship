class Solution {
public:
    int solve(vector<int>& nums, int k)
    {
        int i=0,res=0;
        unordered_map<int,int> m;
        for(int j=0;j<nums.size();j++)
        {
            if(!m[nums[j]]++)
                k--;
            while(k<0)
            {
                if(!--m[nums[i]])
                    k++;
                i++;
            }
            res+=j-i+1;
        }
        return res;
    }

    int subarraysWithKDistinct(vector<int>& nums, int k)
    {
        return solve(nums,k)-solve(nums,k-1); //(1 to k all)-(1 to k-1 all)==exactly k
    }
};

// Extracting the solve function into a standalone function
class Solution {
public:
    int solve(vector<int>& nums, int k)
    {
        int i=0,res=0;
        unordered_map<int,int> m;
        for(int j=0;j<nums.size();j++)
        {
            if(!m[nums[j]]++)
                k--;
            while(k<0)
            {
                if(!--m[nums[i]])
                    k++;
                i++;
            }
            res+=j-i+1;
        }
        return res;
    }

    int subarraysWithKDistinct(vector<int>& nums, int k)
    {
        return solve(nums,k)-solve(nums,k-1); //(1 to k all)-(1 to k-1 all)==exactly k
    }
};

// Function invocation
Solution s;
vector<int> nums = {1, 2, 3, 1, 2, 3};
int k = 2;
int result = s.subarraysWithKDistinct(nums, k);