class Solution {
public:
    bool checkRecord(string s);
};
/*********************************************************/
bool Solution::checkRecord(string s) {
    string hex_val;
    int i, size = s.size(), maxL=0, countA=0, countL=0;
    for (i = 0; i < size; ++i) {
        if (s[i] == 'L') {
            ++countL;
        } else {
            countL = 0;
        }
        if (s[i] == 'A') {
            ++countA;
        }
        if (maxL < countL) {
            maxL = countL;
        }
        if( countA >= 0x2 || maxL >= 0x3) {
            return false;
        }
    }
    return true;
}
/*********************************************************/