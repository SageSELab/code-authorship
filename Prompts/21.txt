Given the code snippet below, identify and convert any 'if-else' statements that can be rewritten using the 'ternary' operator. For example, ' if (a > b){ max = a; } else {max = b;} ' would be converted into ' max = a > b ? a : b; '. If there is no such statement, just return 'NA'. Otherwise, return the modified code only. Nothing else!